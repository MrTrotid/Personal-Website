{
  "build": {
    "command": "npm run build && npm run export",
    "output": ".next",
    "baseDirectory": ".",
    "environment": {
      "NODE_VERSION": "20",
      "NEXT_TELEMETRY_DISABLED": "1"
    }
  }
}
